{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "rm -r build ag_data_lib.egg-info dist && python setup.py sdist bdist_wheel && twine check dist/*",
			"presentation": {
				"close": true
			}
        }
    ]
}